# TopView

Electron desktop application built with React, Vite, and TypeScript.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The source is separated into Electron main, preload, and React renderer layers under `src/`.
